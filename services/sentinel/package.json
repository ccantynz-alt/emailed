{
  "name": "@alecrae/sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered zero-latency validation pipeline \u2014 the immune system of AlecRae",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "echo 'Service builds independently \u2014 not part of Vercel web deployment'",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@alecrae/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
