apiVersion: v1
kind: ConfigMap
metadata:
  name: alecrae-config
  namespace: alecrae
  labels:
    app.kubernetes.io/name: alecrae-config
    app.kubernetes.io/part-of: alecrae-platform
data:
  # ─── Service URLs (internal cluster DNS) ───────────────────────────────────
  API_INTERNAL_URL: "http://api.alecrae.svc.cluster.local"
  CLICKHOUSE_URL: "http://clickhouse.alecrae.svc.cluster.local:8123"
  MEILI_URL: "http://meilisearch.alecrae.svc.cluster.local:7700"
  S3_ENDPOINT: "https://s3.amazonaws.com"

  # ─── Public URLs ───────────────────────────────────────────────────────────
  WEB_URL: "https://app.alecrae.com"
  API_URL: "https://api.alecrae.com"

  # ─── S3 / Object Storage ──────────────────────────────────────────────────
  S3_BUCKET: "alecrae-attachments"
  S3_REGION: "us-east-1"

  # ─── MTA Configuration ────────────────────────────────────────────────────
  MTA_HOSTNAME: "mail.alecrae.com"
  DKIM_SELECTOR: "default"

  # ─── CORS ─────────────────────────────────────────────────────────────────
  CORS_ORIGINS: "https://app.alecrae.com,https://admin.alecrae.com"

  # ─── Observability ────────────────────────────────────────────────────────
  OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector.monitoring.svc.cluster.local:4318"
  OTEL_SERVICE_NAME: "alecrae"
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"

  # ─── Feature Flags ────────────────────────────────────────────────────────
  ENABLE_AI_COMPOSE: "true"
  ENABLE_SMART_PRIORITY: "true"
  ENABLE_RELATIONSHIP_GRAPH: "true"

  # ─── Rate Limiting ────────────────────────────────────────────────────────
  RATE_LIMIT_WINDOW_MS: "60000"
  RATE_LIMIT_MAX_REQUESTS: "100"
  API_RATE_LIMIT_MAX: "500"

  # ─── Email Processing ─────────────────────────────────────────────────────
  MAX_ATTACHMENT_SIZE_MB: "25"
  MAX_RECIPIENTS_PER_MESSAGE: "500"
  BOUNCE_THRESHOLD_PERCENT: "5"
