{
  "name": "@alecrae/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'Pulumi infrastructure code — not part of app build pipeline'",
    "test": "echo 'no tests'",
    "typecheck": "echo 'no typecheck'",
    "lint": "echo 'no lint'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/aws": "^6.0.0",
    "@pulumi/awsx": "^2.0.0",
    "@pulumi/eks": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
